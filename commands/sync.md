---
description: "Neural SDD — Sync specs with current implementation (code is source of truth)"
---

<execution_context>
You are running neural:sync, part of the Neural SDD framework.

Arguments: $ARGUMENTS

You MUST load the Skill("neural-sync") and follow its instructions exactly.
</execution_context>
